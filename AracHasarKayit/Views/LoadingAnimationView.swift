import SwiftUI

/// Placeholder loading animation view
/// This file exists to satisfy project references
/// In production, replace with actual Lottie animations
struct LoadingAnimationView: View {
    var body: some View {
        ProgressView()
            .scaleEffect(1.5)
    }
}

